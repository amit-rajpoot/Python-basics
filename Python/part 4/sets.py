# <---SETS--->
# Set is the collection of the inordered items.
# Each element is the set must be unique & immitable.
# We can easily store->int,boolen,float,string,tuple(but list and dictionary can not be store.)
# For storing the set we closed this with {curly} and seprate with comma.
# NOTE--->sets mai ham agar do bar ek hi value ko input karte hai toh set usse one time hi considerd karta hai.
# NOTE--->sets toh mutable hai but uske elemnets immutable hai.
# EX..1
collection = {1, 2, 3, 4, 4, 4, 4}
print(collection)
print(type(collection))
#For create empty we use syntax-->
collection_null = set()
print(type(collection_null))














